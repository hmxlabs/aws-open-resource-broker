"""Kubernetes provider services package.

Houses the infrastructure discovery service, discovery data models,
interactive init-prompt helpers, the handler registry, capability service,
health-check service, and instance-operation service for the k8s provider.
"""
