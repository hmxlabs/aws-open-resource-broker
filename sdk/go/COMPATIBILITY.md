# Compatibility Matrix

The Go SDK and Python service are released together from this repo and share the same version tag.

| Go SDK | Python service | Notes |
|--------|----------------|-------|
| v1.6.2 | v1.6.2 | Initial Go SDK release |

The Go SDK's `MinCompatibleVersion` (in `orb/version.go`) is updated as part of every release.
